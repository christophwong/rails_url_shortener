<h1>Shorten your urls</h1>

<% if @url.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@url.errors.count, "error") %> prohibited
      this url from being saved:</h2>
    <ul>
    <% @url.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

<%= form_for @url do |f| %>
  <%= f.label :url %><br>
  <%= f.text_field :url %><br>
  <%= f.submit %>
<% end %>
